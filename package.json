{
  "name": "blockchain-swap-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=blockchain-swap-frontend",
    "build": "npm run build --workspace=blockchain-swap-frontend",
    "start": "npm run start --workspace=blockchain-swap-frontend",
    "lint": "npm run lint --workspace=blockchain-swap-frontend"
  }
}
